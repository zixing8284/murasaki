---
"@murasaki-io/react98": minor
---

Add a `flat` variant to `Button` for flat toolbar buttons (transparent at rest,
raised on hover, sunken while pressed) and a `hideToggle` prop to `TreeViewItem`
for a toggle-less namespace root whose children are always shown.

Also center the icon in `iconOnly` `Button`s (an inline icon previously sat on
the text baseline) and tighten the `WindowMenuBar` trigger line-height so the
bar no longer carries extra vertical padding.
