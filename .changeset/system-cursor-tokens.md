---
"@murasaki-io/react98": minor
---

Add `--cursor-default` and `--cursor-pointer` CSS custom properties with matching Tailwind utilities, enabling consumers to theme pointer feedback library-wide. Scrollbar primitives now use these tokens instead of hardcoded `cursor: default`.
