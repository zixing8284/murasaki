# Murasaki React98

Murasaki React98 is a Windows 98-themed React component library. This documentation site is the source of truth for component reference pages, examples, and usage guidance.

<div className="m98-home-grid">
  <section className="m98-doc-card">
    <h2>Component Reference</h2>
    <p>Browse component APIs, behavior notes, accessibility guidance, keyboard support, and SSR expectations.</p>
    <p><a href="/programs/docs/components">Open the component index</a></p>
  </section>
  <section className="m98-doc-card">
    <h2>Examples</h2>
    <p>Examples are maintained as ordinary TSX modules so they can be typed, linted, rendered, and embedded without generated sidecars.</p>
  </section>
</div>

## Documentation Shape

Every public component has a dedicated reference page with live examples, source snippets, accessibility notes, and SSR expectations. The playground embeds this static docs build as an iframe so the docs system can stay independent from the desktop demo runtime.
