# Checkbox

The `Checkbox` component renders a Windows 98 styled checkbox with a label.
It uses a hidden native `<input type="checkbox">` with CSS pseudo-elements to render the classic checkmark appearance.

## Props

import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from 'murasaki-react98'
import { CodeBlock } from '../../directory/apps/docs/code-block'

<Table>
  <TableHeader>
    <TableRow>
      <TableHead>Prop</TableHead>
      <TableHead>Type</TableHead>
      <TableHead>Default</TableHead>
      <TableHead>Description</TableHead>
    </TableRow>
  </TableHeader>
  <TableBody>
    <TableRow>
      <TableCell>`checked`</TableCell>
      <TableCell>`boolean`</TableCell>
      <TableCell>—</TableCell>
      <TableCell>Controlled checked state</TableCell>
    </TableRow>
    <TableRow>
      <TableCell>`defaultChecked`</TableCell>
      <TableCell>`boolean`</TableCell>
      <TableCell>`false`</TableCell>
      <TableCell>Initial checked state (uncontrolled)</TableCell>
    </TableRow>
    <TableRow>
      <TableCell>`disabled`</TableCell>
      <TableCell>`boolean`</TableCell>
      <TableCell>`false`</TableCell>
      <TableCell>Disables the checkbox</TableCell>
    </TableRow>
    <TableRow>
      <TableCell>`onChange`</TableCell>
      <TableCell>`(e) => void`</TableCell>
      <TableCell>—</TableCell>
      <TableCell>Change handler</TableCell>
    </TableRow>
    <TableRow>
      <TableCell>`children`</TableCell>
      <TableCell>`ReactNode`</TableCell>
      <TableCell>—</TableCell>
      <TableCell>Label content</TableCell>
    </TableRow>
    <TableRow>
      <TableCell>`className`</TableCell>
      <TableCell>`string`</TableCell>
      <TableCell>—</TableCell>
      <TableCell>Additional CSS classes</TableCell>
    </TableRow>
  </TableBody>
</Table>

## Usage

<CodeBlock code={`import { Checkbox } from 'murasaki-react98'

<Checkbox>Enable notifications</Checkbox>
<Checkbox defaultChecked>Remember me</Checkbox>
<Checkbox disabled>Unavailable option</Checkbox>`} />
