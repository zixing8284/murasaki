import { ShowCode } from "@docs/mdx-components";

import { BasicDemo } from "./demos/basic";
import BasicSource from "./demos/basic.tsx?raw";
import { InteractiveDemo } from "./demos/interactive";
import InteractiveSource from "./demos/interactive.tsx?raw";
import { ScrollableDemo } from "./demos/scrollable";
import ScrollableSource from "./demos/scrollable.tsx?raw";
import { WithFooterDemo } from "./demos/with-footer";
import WithFooterSource from "./demos/with-footer.tsx?raw";

# Table

> To render a table view, use a table element. Wrap it with a div element with sunken-panel class to provide proper border and overflow container.
>
> — _98.css Documentation_

## Quick Start

```tsx
import {
  Table,
  TableBody,
  TableCell,
  TableHead,
  TableHeader,
  TableRow,
  TableFooter,
} from "#/components/table/table";

// Basic table with fixed size and internal scrollbar
<Table containerClassName="h-50 w-100">
  <TableHeader>
    <TableRow>
      <TableHead>Name</TableHead>
      <TableHead>Version</TableHead>
      <TableHead>Company</TableHead>
    </TableRow>
  </TableHeader>
  <TableBody>
    <TableRow>
      <TableCell>MySQL ODBC 3.51 Driver</TableCell>
      <TableCell>3.51.11.00</TableCell>
      <TableCell>MySQL AB</TableCell>
    </TableRow>
  </TableBody>
</Table>

// Interactive table with row selection
const [selectedId, setSelectedId] = useState<string | null>(null);
<Table containerClassName="h-50 w-125">
  <TableHeader>
    <TableRow>
      <TableHead>Name</TableHead>
      <TableHead>Version</TableHead>
    </TableRow>
  </TableHeader>
  <TableBody>
    {data.map((item) => (
      <TableRow
        key={item.id}
        selected={selectedId === item.id}
        onClick={() => setSelectedId(item.id)}
      >
        <TableCell>{item.name}</TableCell>
        <TableCell>{item.version}</TableCell>
      </TableRow>
    ))}
  </TableBody>
</Table>
```

## Props

### Table

| Prop               | Type                         | Required | Default     | Description                                          |
| ------------------ | ---------------------------- | -------- | ----------- | ---------------------------------------------------- |
| `containerClassName` | `string`                   | No       | -         | Classes for the outer container (use for width/height) |
| `className`        | `string`                     | No       | -           | Additional CSS classes for the table element         |

### TableRow

| Prop        | Type      | Required | Default | Description                                           |
| ----------- | --------- | -------- | ------- | ----------------------------------------------------- |
| `selected`  | `boolean` | No       | `false` | Highlights the row with Windows 98 blue background    |
| `className` | `string`  | No       | -       | Additional CSS classes                                |

### TableHead, TableCell, TableHeader, TableBody, TableFooter, TableCaption

Standard table section components with full `className` support.

## Examples

### Basic

<BasicDemo />

<ShowCode source={BasicSource} />

### Scrollable

Table with internal scrollbar for handling large datasets.

<ScrollableDemo />

<ShowCode source={ScrollableSource} />

### Interactive

Use the `selected` prop on TableRow to enable row selection.

<InteractiveDemo />

<ShowCode source={InteractiveSource} />

### With Footer

Use `TableFooter` to display summary information.

<WithFooterDemo />

<ShowCode source={WithFooterSource} />

## Integration with TanStack Table

The Table component is designed to work seamlessly with [@tanstack/react-table](https://tanstack.com/table/latest):

```tsx
import {
  useReactTable,
  getCoreRowModel,
  getSortedRowModel,
  flexRender,
} from "@tanstack/react-table";

export function DataTable() {
  const table = useReactTable({
    data,
    columns,
    getCoreRowModel: getCoreRowModel(),
    getSortedRowModel: getSortedRowModel(),
  });

  return (
    <Table>
      <TableHeader>
        {table.getHeaderGroups().map((headerGroup) => (
          <TableRow key={headerGroup.id}>
            {headerGroup.headers.map((header) => (
              <TableHead key={header.id}>
                {flexRender(
                  header.column.columnDef.header,
                  header.getContext()
                )}
              </TableHead>
            ))}
          </TableRow>
        ))}
      </TableHeader>
      <TableBody>
        {table.getRowModel().rows.map((row) => (
          <TableRow key={row.id}>
            {row.getVisibleCells().map((cell) => (
              <TableCell key={cell.id}>
                {flexRender(cell.column.columnDef.cell, cell.getContext())}
              </TableCell>
            ))}
          </TableRow>
        ))}
      </TableBody>
    </Table>
  );
}
```
